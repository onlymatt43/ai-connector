# ============================================
# AI Connector - Configuration d'environnement
# ============================================
# Copier ce fichier en .env et remplir les valeurs

# ============================================
# OPENAI CONFIGURATION (OBLIGATOIRE)
# ============================================
# Clé API OpenAI pour accéder aux modèles GPT
# Obtenir sur: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Modèle OpenAI par défaut
# Options: gpt-4o, gpt-4o-mini, gpt-4-turbo, gpt-3.5-turbo
# Default: gpt-4o-mini (économique et rapide)
OPENAI_MODEL=gpt-4o-mini

# ============================================
# CORS & SÉCURITÉ
# ============================================
# Liste des origines autorisées séparées par virgule (CORS)
# Exemple: https://onlymatt.ca,https://www.onlymatt.ca,https://video.onlymatt.ca
# Utiliser "*" pour autoriser toutes les origines (non recommandé en production)
ALLOWED_ORIGINS=https://onlymatt.ca,https://www.onlymatt.ca

# ============================================
# TIMEOUTS (optionnel)
# ============================================
# Timeout de connexion à l'API OpenAI (en secondes)
# Default: 10
LLM_TIMEOUT_CONNECT=10

# Timeout de lecture de la réponse OpenAI (en secondes)
# Augmenter si vous utilisez des requêtes complexes
# Default: 70
LLM_TIMEOUT_READ=70

# ============================================
# METADATA SERVICE (optionnel)
# ============================================
# Nom du service (pour les logs et métriques)
# Default: varie selon le service
APP_NAME=hey-hi-coach-onlymatt

# Version de l'application
# Default: v2-resilient
APP_VERSION=v2-resilient

# ============================================
# CIRCUIT BREAKER & RESILIENCE (optionnel)
# ============================================
# Ces valeurs sont configurées dans shared/chat_proxy.py
# Pour les modifier, éditer directement le code:
# - MAX_RETRIES=3 (nombre de tentatives)
# - INITIAL_BACKOFF=1.0 (délai initial entre retries en secondes)
# - Circuit breaker: 5 échecs ouvrent le circuit pendant 60s

# ============================================
# VALIDATION LIMITS (optionnel)
# ============================================
# Ces limites sont configurées dans shared/chat_proxy.py
# - MAX_MESSAGE_LENGTH=50000 (caractères par message)
# - MAX_MESSAGES_COUNT=100 (nombre max de messages)

# ============================================
# WORDPRESS CONNECTOR (si utilisé)
# ============================================
# URL de base du service Core AI
HEYHI_CORE_AI_BASE=https://hey-hi-assistant-core-onlymatt.onrender.com

# Clé API WordPress pour sécuriser les appels
HEYHI_WP_API_KEY=votre-cle-secrete-wordpress

# Limite de requêtes par minute
HEYHI_RATE_LIMIT_PER_MIN=120

# Mode debug (active les logs détaillés)
# 0 = désactivé, 1 = activé
HEYHI_DEBUG=0

# ============================================
# RENDER.COM DEPLOYMENT (si hébergé sur Render)
# ============================================
# Ces variables sont configurées dans le dashboard Render
# Port d'écoute (automatique sur Render)
# PORT=10000

# ============================================
# NOTES
# ============================================
# 1. Ne jamais commiter ce fichier avec de vraies valeurs
# 2. Les variables marquées (OBLIGATOIRE) sont requises
# 3. Les services démarrent avec des valeurs par défaut sécurisées
# 4. Consulter README.md pour plus d'informations
